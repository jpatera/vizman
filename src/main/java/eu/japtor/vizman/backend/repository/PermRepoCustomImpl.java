package eu.japtor.vizman.backend.repository;

import eu.japtor.vizman.backend.entity.Perm;

//@Repository
public class PermRepoCustomImpl implements PermRepoCustom {

//    @PersistenceContext
//    EntityManager entityManager;
//
//    @Override
//    public Perm findByName(final String name) {
//
//        Query query = entityManager.createNativeQuery("SELECT role.* FROM Perm as perm " +
//                "WHERE perm.name = ?", Perm.class);
//        query.setParameter(1, name);
////        query.setParameter(1, firstName + "%");
//        List<Perm> perms = (List<Perm>) query.getResultList();
//        // ... = query.getSingleResult();
//        if (perms.isEmpty()) {
//            return null;
//        } else {
//            return perms.get(0);
//        }
//    }
}
